# Shared dependency set for every service in the platform.
# Installed by each service Dockerfile from the repository root.

# --- web / services -------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.1
httpx>=0.27.0

# --- streaming ------------------------------------------------------------
aiokafka>=0.10.0

# --- persistence ----------------------------------------------------------
SQLAlchemy>=2.0.30
psycopg2-binary>=2.9.9

# --- kubernetes collectors ------------------------------------------------
kubernetes>=29.0.0

# --- ai inference ---------------------------------------------------------
openai>=1.40.0

# --- dashboard ------------------------------------------------------------
streamlit>=1.50.0
pandas>=2.0.0
requests>=2.31.0

# --- tests ----------------------------------------------------------------
pytest>=8.0.0
